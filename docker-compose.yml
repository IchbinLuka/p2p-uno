services:
    uno-server:
        build:
            context: .
        volumes:
            - .:/app
        ports:
            - "8080:8080"
            - "8000:8000"
        # command: /bin/bash -c "/scripts/start.sh"
